vectorDotProduct(vectorA, VectorB); //all vector components must be numbers
vectorCrossProduct(vectorA, VectorB); //vectors must have 3 components, all vector components must be numbers
transpose(matrix); must be a square matrix, //all matrix components must be numbers
transform(vector, matrix); //for vector with n components and matrix must be nxn, all vector and matrix components must be numbers
vectorLength(vector); //all vector components must be numbers
